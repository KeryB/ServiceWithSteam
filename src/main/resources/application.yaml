spring:
  datasource:
    url: jdbc:mysql://localhost:3306/service_steam
    username: root
    password: root
  jpa:
    database: mysql
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
jwt:
  header: x-auth-token
  secret: da7ca69f41dce5179752a7546dfa0959
  expiration: 259200
